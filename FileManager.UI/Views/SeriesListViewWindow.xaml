<UserControl x:Class="FileManager.UI.Views.SeriesListViewWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FileManager.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<ListView Margin="10" Name="lvSeries" ItemsSource="{Binding SeriesList}">
			<ListView.View>
				<GridView>
					<GridViewColumn Header="Series Id" Width="auto" DisplayMemberBinding="{Binding EpisodeId}"/>
					<GridViewColumn Header="Name" Width="auto" DisplayMemberBinding="{Binding Name}"/>
				</GridView>
			</ListView.View>
		</ListView>
	</Grid>
</UserControl>